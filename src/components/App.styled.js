import styled from '@emotion/styled';

export const Container = styled.div`
  padding: 0 16px;
`;

export const Title = styled.h2`
  margin: 0;
  padding: 24px 0;
  font-weight: 400;
  font-size: 36px;
`;
